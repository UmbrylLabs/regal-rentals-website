{
  "name": "regal-rentals-website",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "python3 -m unittest -v tests/test_inventory_guards.py && node tests/test_inventory_policy.mjs && node tests/test_event_time.mjs && node tests/test_rentals_ui.mjs",
    "check:js": "find functions admin -name '*.js' -print0 | xargs -0 -n1 node --check && node --check event-time.js && node --check rentals.js && node --check sign.js",
    "dev": "wrangler pages dev .",
    "db:migrate:local": "wrangler d1 migrations apply regal-rentals --local",
    "db:migrate:remote": "wrangler d1 migrations apply regal-rentals --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
